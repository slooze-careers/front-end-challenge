/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow hot module replacement requests from your local network IP
  allowedDevOrigins: ['192.168.68.121', 'localhost'],
};

export default nextConfig;
